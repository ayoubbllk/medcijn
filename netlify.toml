# Déploiement de la version HTML statique (dossier static/)
# Désactive le runtime Next.js installé via l'UI Netlify.
[build]
  publish = "static"
  command = "echo Static site ready"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
